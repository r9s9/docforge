"""assembler — deterministic DOCX rendering from a template + context."""

from __future__ import annotations

from .assembler import assemble, build_render_context

__all__ = ["assemble", "build_render_context"]
