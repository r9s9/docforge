# Runtime dependencies for the Vercel Python Function (the FastAPI backend).
# Mirrors pyproject's runtime deps, plus the Postgres driver (Supabase), and
# omits the local-only ASGI server (Vercel provides the server) and dev tooling.
# Keep this in sync with backend/pyproject.toml [project].dependencies.

fastapi>=0.110
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

SQLAlchemy>=2.0

python-docx>=1.1
docxtpl>=0.17
lxml>=5.1
Jinja2>=3.1

PyJWT[crypto]>=2.8
httpx>=0.27
python-dateutil>=2.9
Pillow>=10.2

# Managed Postgres (Supabase). Binary wheel = no build step on Vercel.
psycopg2-binary>=2.9
